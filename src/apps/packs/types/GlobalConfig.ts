export interface GlobalConfig {
    /**
     * Path to FNIS folder which is needed to build behavior files
     * It works with default FNIS folder structure and looks for GenerateFNISforModders.exe and generated for module hkx behavior file
     */
    fnisForModdersPath: string
}